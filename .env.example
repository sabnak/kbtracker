# Database
# Directory holding the SQLite database files (app.db + per-game game_N.db).
# Defaults to <project_root>/db when unset.
DATABASE_DIR=/app/db

# Application
APP_LOCAL_PORT=9333
GAME_DATA_PATH=/data
